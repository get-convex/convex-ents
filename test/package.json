{
  "name": "tests",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "npx convex run read:runTests && npx convex run write:runTests",
    "lint": "tsc && eslint ."
  },
  "dependencies": {
    "@storybook/expect": "^28.1.3-5",
    "convex": "^1.8.0-alpha.0",
    "convex-helpers": "^0.1.12"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.16.0",
    "eslint": "8.49.0"
  }
}
